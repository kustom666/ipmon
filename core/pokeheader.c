#include "pokeheader.h"

pokeheader serialize_header(char *instream)
{
	// if(strlen(instream) != 48)
	// {
	// 	perror("Le header envoyé à une taille incorrecte");
	// }

	
		char bufftype[4];
		bufftype[0] = instream[0];
		bufftype[0] = instream[0];

}